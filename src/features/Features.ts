type ButtonNamesProps = {
    inc: string
    reset: string
    set: string
}
type ErrorStatusesProps = {
    incorrect: string
    change: string
}
export const ButtonNames: ButtonNamesProps = {
    inc: 'inc',
    reset: 'reset',
    set: 'set'
}
export const ErrorStatuses: ErrorStatusesProps = {
    incorrect: 'incorrect value',
    change: 'value change'
}